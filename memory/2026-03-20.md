# 2026-03-20

- Bootstrapped XConnect repository from empty workspace.
- Implemented Rust workspace with shared protocol crate and serde roundtrip tests.
- Added control-plane service (Axum) with auth, devices, sessions, websocket signaling, and in-memory persistence.
- Added deployment artifacts for Ubuntu 22.04 self-host stack: Docker Compose, Nginx TLS, coturn, certbot bootstrap/renew scripts.
- Added host/viewer runtime crate interfaces and desktop Tauri shell scaffold.
- Could not run cargo-based verification because Rust toolchain is missing in this environment.
- Phase 2 implemented: real WebRTC offer/answer + ICE candidate handling + WS signaling integration for host/viewer runtimes.
- Added data channels: input (viewer -> host) and clipboard (bi-directional over channel created by host).
- Host runtime now applies remote input via enigo and syncs system clipboard via arboard.
- Windows/macOS capture backends now use xcap monitor capture (first display).
- Signaling server now validates signal envelope JSON and session_id matching before relaying.
- Added unit tests for viewer input normalization and signaling session matching logic.
- Still unable to run cargo test/check due missing Rust toolchain on this machine.
- Installed Rust toolchain via rustup-init and verified cargo/rustc available.
- Verification note: cargo builds/tests fail if environment variables INCLUDE/LIB point to legacy VS 2003 SDK paths; clearing them for command scope (`$env:INCLUDE=''; $env:LIB=''`) resolves native dependency builds.
- Final verification passed: cargo fmt --check, cargo check for control-plane/host-runtime/viewer-runtime, and cargo test for protocol/control-plane/host-runtime/viewer-runtime.
- Added viewer on_track RTP packet receiver API (`recv_video_packet`) to complete video track consumption path.
- Final verification succeeded after formatting:
  - cargo fmt --all --check
  - cargo check -p control-plane -p host-runtime -p viewer-runtime (with INCLUDE/LIB cleared)
  - cargo test -p xconnect-protocol -p control-plane -p host-runtime -p viewer-runtime (with INCLUDE/LIB cleared)
- Refactored host encoder into multi-backend selection: software + Windows(Media Foundation/NVENC/QSV entrypoints) + macOS(VideoToolbox probe).
- Added runtime backend selection via `XCONNECT_H264_BACKEND` env var and `active_backend()` introspection.
- Wired Media Foundation initialization (MFStartup/COM init) on Windows and VideoToolbox hardware capability probe on macOS.
- Kept software payload fallback to preserve stable runtime while preparing full hardware bitstream implementation.
- Updated README with backend selection and current encoder status.
- Verified: cargo fmt --check, cargo check, cargo test (protocol/control-plane/host-runtime/viewer-runtime) all pass with INCLUDE/LIB cleared.
- Implemented concrete H.264 encoder backends:
  - Windows: real Media Foundation hardware MFT encode path (NV12 input -> H.264 output), plus vendor-filtered NVENC/QSV selection paths via hardware MFT enumeration.
  - macOS: real VideoToolbox `VTCompressionSession` encode path producing H.264 Annex-B NAL units.
- Added Annex-B conversion helpers (AVCC length-prefix -> Annex-B), NV12 conversion from RGBA, and encoder unit test for AVCC conversion.
- Re-verified after implementation:
  - `cargo fmt --all`
  - `cargo check -p control-plane -p host-runtime -p viewer-runtime`
  - `cargo test -p xconnect-protocol -p control-plane -p host-runtime -p viewer-runtime`
