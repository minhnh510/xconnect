# Runtime TURN config is provided by docker-compose command flags.
# Keep this file so deployments can mount a stable config path and override if needed.
# For reference defaults:
# listening-port=3478
# tls-listening-port=5349
# min-port=49152
# max-port=65535
# use-auth-secret
